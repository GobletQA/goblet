

/**
 * Action to store the session container meta data in the store
 * May also store it in local-storage, need to investigate
 * 
 */
export const setContainer = (status) => {
  console.log(`------- TODO: set container status -------`)
  console.log(status)
}