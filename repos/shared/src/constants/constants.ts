export const ARTIFACT_TYPES = [
    `traces`,
    `videos`,
    `snapshots`,
    `downloads`
  ]

export const ARTIFACT_SAVE_OPTS = {
  true: true,
  false: false,
  always: `always`,
  failed: `failed`,
  never: `never`,
}

export const DefinitionOverrideFolder = `override`