module.exports = {
  ARTIFACT_TYPES: [
    `traces`,
    `videos`,
    `snapshots`,
    `downloads`
  ],
  ARTIFACT_SAVE_OPTS: {
    true: true,
    false: false,
    always: `always`,
    failed: `failed`,
    never: `never`,
  }
}