import type { TRaceModels } from '@GBR/types'

import Box from '@mui/material/Box'

export type TSidebar = {
  models: TRaceModels
}

export const Sidebar = (props:TSidebar) => {

  return (
    <Box>
      Sidebar
    </Box>
  )
  
}