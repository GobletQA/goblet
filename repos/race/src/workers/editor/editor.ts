/// <reference lib="webworker" />
declare const self: DedicatedWorkerGlobalScope;

import type { TRaceFeature } from '@GBR/types'
import type { TStepDefsList, TWorldConfig } from '@ltipton/parkin'

export const auditFeature = async () => {
  
}