export enum ELatentEnv {
  qa=`qa`,
  local=`local`,
  test=`test`,
  staging=`staging`,
  develop=`develop`,
  production=`production`,
}

export enum ELoadFormat {
  object=`object`,
  string=`string`
}

export enum EFileType {
  values=`values`,
  secrets=`secrets`
}