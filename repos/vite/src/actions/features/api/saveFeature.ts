import type { TFeatureFileModel } from '@types'

/**
 * Saves a feature fileModel to the local file system by calling the backend API
 * @type function
 * @param {Object} feature - FileModel representing the feature
 *
 * @returns {void}
 */
export const saveFeature = (feature:TFeatureFileModel) => {
  // TODO
  return null
}
