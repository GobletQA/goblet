import { noOpObj, isObj } from '@keg-hub/jsutils'

/**
 * Updates the store with the passed in browser status
 *
 * @returns {Void}
 */
export const setBrowserStatus = (status:Record<any, any> = noOpObj) => {

}
