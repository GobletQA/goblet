import { noOpObj, isObj } from '@keg-hub/jsutils'

/**
 * Updates the store with the passed in status
 *
 * @returns {Object} - Response from the backend API
 */
export const setSCStatus = (status:Record<any, any>) => {

}
