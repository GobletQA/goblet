import Box from '@mui/material/Box'
import { styled } from '@mui/material/styles'
import { dims } from '@gobletqa/components/theme'


export const SnapshotContainer = styled(Box)(({ theme }) => {
  return ``
})


export const SnapshotCanvasContainer = styled(Box)(({ theme }) => {
  return ``
})

export const SnapshotCanvas = styled(`canvas`)(({ theme }) => {
  return ``
})

export const SnapshotElement = styled(Box)(({ theme }) => {
  return `
    user-select: none;
    position: absolute;
  `
})

export const SnapshotElTools = styled(Box)(({ theme }) => {
  return `
  `
})

