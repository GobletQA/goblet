import Container from '@mui/material/Container'

export type TScreencastProps = {
  
}

export const Screencast = (props:TScreencastProps) => {
  
  return (
    <Container sx={{ backgroundColor: `#9a9a9a` }} >
      Screencast
    </Container>
  )
  
}