import { Auto } from './Auto'
import { Button } from './Button'
import { Input } from './Input'
import { Select } from './Select'
import { Slider } from './Slider'
import { TextArea } from './TextArea'

import { Radio, Checkbox, Switch } from './Toggles'

export const InputTypes = {
  Auto,
  Input,
  Radio,
  Switch,
  Slider,
  Select,
  Button,
  Checkbox,
  TextArea,
  TextField: Input,
  auto: Auto,
  input: Input,
  radio: Radio,
  switch: Switch,
  slider: Slider,
  select: Select,
  button: Button,
  textarea: TextArea,
  textArea: TextArea,
  checkbox: Checkbox,
  textfield: Input,
  textField: Input,
  text: Input,
}