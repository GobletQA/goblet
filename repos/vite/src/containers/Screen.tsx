import Box from '@mui/material/Box'
import { ReactNode } from 'react'

type TScreenProps = {
  children?: ReactNode
}

export const Screen = (props:TScreenProps) => {
  return (
    <Box>
      Screen
    </Box>
  )
}