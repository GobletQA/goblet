const paddingSize = 20
const marginSize = 20

export const gutter = {
  padding: {
    size: paddingSize,
    px: `${paddingSize}px`
  },
  margin: {
    size: marginSize,
    px: `${marginSize}px`
  }
}
