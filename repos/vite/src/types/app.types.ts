
export enum EEditorType {
  visual = `visual`,
  Visual = `visual`,
  VISUAL = `visual`,
  code = `code`,
  Code = `code`,
  CODE = `code`,
}

export enum EAppStatus {
  Idle=`idle`,
  Active=`active`,
  Prompted=`prompted`,
  Shutdown=`shutdown`
}
