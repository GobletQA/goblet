
export enum EEditorType {
  visual = `visual`,
  Visual = `visual`,
  VISUAL = `visual`,
  code = `code`,
  Code = `code`,
  CODE = `code`,
}

export enum EAppStatus {
  Idle=`Idle`,
  Active=`Active`,
  Prompted=`Prompted`
}
