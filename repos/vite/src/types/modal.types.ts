
export enum EModalTypes {
  Idle = `idle`,
  IDLE = `idle`,
  idle = `idle`,
  Artifacts = `artifacts`,
  ARTIFACTS = `artifacts`,
  artifacts = `artifacts`,
  Confirm = `confirm`,
  CONFIRM = `confirm`,
  confirm = `confirm`,
  Connect = `connect`,
  CONNECT = `connect`,
  connect = `connect`,
  Settings = `settings`,
  SETTINGS = `settings`,
  settings = `settings`,
  SignIn = `signIn`,
  SIGN_IN = `signIn`,
  signIn = `signIn`,
  Environments = `environments`,
  environments = `environments`,
  ENVIRONMENTS = `environments`
} 


